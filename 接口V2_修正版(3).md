# 股票交易系统五组接口约定 v0.2

本文档用于约定五个小组之间的跨模块接口，作为联调、Mock、测试用例和后续代码实现的共同依据。

V2 变更重点：

- 补充账户关联校验接口返回字段，明确证券账户与资金账户一对一绑定关系。
- 规范资金账户密码类型 `password_type`，区分交易密码 `TRADE` 与取款密码 `WITHDRAW`。
- 补充账户关联查询状态 `association_status`，支持 `ACTIVE` 与 `UNLINKED`。
- 根据讨论结果明确：INFO 每 5 秒 REST 拉取行情，WebSocket 作为可选优化。
- 明确 ADMIN 向 TRADE 传涨跌停比例，由 TRADE 计算并保存最终价格限制。
- 明确 INFO 独立维护普通/VIP 用户体系，不复用 ACCOUNT 投资者账户。
- 明确 RabbitMQ 是异步实现选项，HTTP/WebSocket 是必需接口。

依据文件：

- `交易系统管理/交易系统管理_需求分析报告.pdf`
- `账户业务子系统/账户业务子系统_需求分析.pdf`
- `网上信息发布/网上信息发布-需求分析报告.pdf`
- `交易客户端/需求报告.pdf`
- `需求分析报告/作业交付/股票中央交易系统需求分析报告.pdf`

## 0. 是否需要 UI

业务 UI 分别由以下模块承担：

- 交易客户端：投资者登录、查询资金/持仓、下单、撤单、查看成交结果。
- 交易系统管理：管理员登录、股票管理、涨跌停设置、暂停/重启交易、审计。
- 网上信息发布：普通/VIP 用户查询行情、查看 K 线、账户注册与升级。

## 1. 技术总约定

### 1.1 总体技术栈

所有后端服务统一按 Python 技术栈约定：

- Python：`3.11+`
- Web 框架：`FastAPI`
- ASGI 服务：`Uvicorn`
- 数据校验：`Pydantic`
- 关系型数据库：`MySQL 8.0+`
- 数据库访问：`SQLAlchemy` 或等价 ORM，推荐驱动为 `PyMySQL`（同步）或 `asyncmy`/`aiomysql`（异步）
- 数据库迁移：推荐 `Alembic`
- 实时推送：`WebSocket`
- 异步消息：`RabbitMQ` 或 Kafka，推荐 RabbitMQ 作为课程项目默认选型
- 接口文档：每组通过 FastAPI 暴露 `/docs` 和 `/openapi.json`

除非某组明确说明只做纯前端页面，后端持久化默认使用 MySQL。各组可以各自维护独立数据库 schema，也可以共用同一 MySQL 实例并通过表名前缀或数据库名隔离，例如 `trade_db`、`account_db`、`admin_db`、`info_db`。

### 1.2 模块代号

| 模块           | 代号    | 主要职责                                                        |
| -------------- | ------- | --------------------------------------------------------------- |
| 交易客户端     | CLIENT  | 投资者侧交互，调用账户、中央交易、发布、管理只读接口            |
| 中央交易系统   | TRADE   | 指令接收、校验、订单簿、撮合、成交、结算消息、行情基础数据      |
| 账户业务子系统 | ACCOUNT | 资金账户、证券账户、账户关联、认证、资金/证券冻结解冻、状态校验 |
| 网上信息发布   | INFO    | 普通/VIP 用户、行情查询、K 线、5 秒行情缓存与展示               |
| 交易系统管理   | ADMIN   | 管理员、权限、涨跌停配置、暂停/重启交易、审计日志               |

### 1.3 命名和数据格式

- URL 统一小写，使用短横线：`/api/v1/trade/orders/{order_id}`。
- JSON 字段统一使用 `snake_case`，与 Python/Pydantic 字段保持一致。
- 所有时间统一使用 ISO 8601 字符串，带时区，例如 `2026-05-25T10:30:00+08:00`。
- 金额、价格统一使用字符串表示十进制数，例如 `"10.05"`，服务端用 `Decimal` 处理，避免浮点误差。
- 数量统一使用整数，单位为股。
- ID 字段统一使用字符串。系统生成 ID 推荐 UUID 或“日期时间 + 序号”。
- 股票代码为 6 位数字字符串，例如 `"600000"`。

### 1.4 通用请求头

| Header                            | 必填              | 说明                               |
| --------------------------------- | ----------------- | ---------------------------------- |
| `Authorization: Bearer <token>` | 除登录/注册外必填 | 用户、管理员或系统服务令牌         |
| `X-Request-Id`                  | 推荐              | 调用方生成的请求追踪 ID            |
| `X-Idempotency-Key`             | 写接口推荐        | 防止重复下单、重复冻结、重复结算   |
| `X-Service-Name`                | 内部接口必填      | 调用方服务名，如 `trade-service` |

### 1.5 通用响应格式

所有 HTTP 接口统一返回：

```json
{
  "success": true,
  "code": "OK",
  "message": "success",
  "data": {},
  "request_id": "req-20260525-0001",
  "timestamp": "2026-05-25T10:30:00+08:00"
}
```

失败响应：

```json
{
  "success": false,
  "code": "TRADE_E05",
  "message": "价格非法或超出涨跌停范围",
  "data": null,
  "request_id": "req-20260525-0001",
  "timestamp": "2026-05-25T10:30:00+08:00"
}
```

### 1.6 分页格式

列表接口统一支持：

| 参数          | 默认   | 说明               |
| ------------- | ------ | ------------------ |
| `page`      | `1`  | 页码，从 1 开始    |
| `page_size` | `20` | 每页数量，最大 100 |

分页返回：

```json
{
  "items": [],
  "page": 1,
  "page_size": 20,
  "total": 0
}
```

## 2. 通用枚举

| 枚举                     | 取值                                                                                                 |
| ------------------------ | ---------------------------------------------------------------------------------------------------- |
| `order_side`           | `BUY`, `SELL`                                                                                    |
| `order_status`         | `ACCEPTED`, `QUEUED`, `PARTIALLY_FILLED`, `FILLED`, `CANCELLED`, `EXPIRED`, `REJECTED` |
| `stock_type`           | `NORMAL`, `ST`                                                                                   |
| `stock_status`         | `OPEN`, `PAUSED`, `CLOSED`                                                                     |
| `account_type`         | `FUND`, `SECURITY`                                                                               |
| `account_status`       | `NORMAL`, `FROZEN`, `LOST`, `CLOSED`                                                         |
| `password_type`        | `TRADE`, `WITHDRAW`                                                                          |
| `association_status`   | `ACTIVE`, `UNLINKED`                                                                         |
| `fund_change_type`     | `FREEZE`, `RELEASE`, `DEDUCT`, `INCREASE`                                                    |
| `position_change_type` | `FREEZE`, `RELEASE`, `DEDUCT`, `INCREASE`                                                    |
| `admin_role`           | `NORMAL_ADMIN`, `SENIOR_ADMIN`, `SYSTEM_ADMIN`, `AUDIT_ADMIN`                                |
| `info_user_level`      | `NORMAL`, `VIP`                                                                                  |
| `kline_period`         | `DAY`, `MONTH`, `YEAR`                                                                         |

## 3. 通用错误码

| 错误码                            | 含义                   |
| --------------------------------- | ---------------------- |
| `COMMON_BAD_REQUEST`            | 请求格式错误           |
| `COMMON_UNAUTHORIZED`           | 未登录或令牌无效       |
| `COMMON_FORBIDDEN`              | 无权限                 |
| `COMMON_NOT_FOUND`              | 资源不存在             |
| `COMMON_CONFLICT`               | 资源状态冲突           |
| `COMMON_INTERNAL_ERROR`         | 服务内部错误           |
| `TRADE_E01`                     | 字段缺失               |
| `TRADE_E02`                     | 股票不存在             |
| `TRADE_E03`                     | 股票不可交易           |
| `TRADE_E04`                     | 买卖方向无效           |
| `TRADE_E05`                     | 价格非法或超出涨跌停   |
| `TRADE_E06`                     | 数量非法               |
| `TRADE_E07`                     | 状态不允许撤销         |
| `ACCOUNT_INSUFFICIENT_FUNDS`    | 可用资金不足           |
| `ACCOUNT_INSUFFICIENT_POSITION` | 可用持仓不足           |
| `ACCOUNT_STATUS_BLOCKED`        | 账户状态不允许当前操作 |
| `INFO_VIP_REQUIRED`             | 当前功能需要 VIP 权限  |
| `ADMIN_STOCK_SCOPE_DENIED`      | 管理员无目标股票权限   |

## 4. 共享数据模型

### 4.1 股票基础信息 `Stock`

| 字段                     | 类型           | 必填 | 说明                               |
| ------------------------ | -------------- | ---- | ---------------------------------- |
| `stock_code`           | string         | 是   | 6 位股票代码                       |
| `stock_name`           | string         | 是   | 股票名称                           |
| `stock_type`           | enum           | 是   | `NORMAL` / `ST`                |
| `previous_close_price` | decimal string | 是   | 昨日收盘价                         |
| `limit_up_price`       | decimal string | 是   | 今日涨停价                         |
| `limit_down_price`     | decimal string | 是   | 今日跌停价                         |
| `stock_status`         | enum           | 是   | `OPEN` / `PAUSED` / `CLOSED` |

### 4.2 行情快照 `MarketQuote`

| 字段               | 类型           | 必填 | 说明           |
| ------------------ | -------------- | ---- | -------------- |
| `stock_code`     | string         | 是   | 股票代码       |
| `stock_name`     | string         | 否   | 股票名称       |
| `latest_price`   | decimal string | 是   | 最新成交价     |
| `open_price`     | decimal string | 否   | 开盘价         |
| `high_price`     | decimal string | 是   | 当日最高价     |
| `low_price`      | decimal string | 是   | 当日最低价     |
| `volume`         | int            | 是   | 当日累计成交量 |
| `turnover`       | decimal string | 否   | 当日成交金额   |
| `best_bid_price` | decimal string | 否   | 当前最优买价   |
| `best_ask_price` | decimal string | 否   | 当前最优卖价   |
| `updated_at`     | datetime       | 是   | 行情更新时间   |

### 4.3 交易指令 `TradeOrder`

| 字段                    | 类型           | 必填 | 说明               |
| ----------------------- | -------------- | ---- | ------------------ |
| `order_id`            | string         | 是   | 指令编号           |
| `investor_id`         | string         | 是   | 投资者编号         |
| `fund_account_id`     | string         | 是   | 资金账户号         |
| `security_account_id` | string         | 是   | 证券账户号         |
| `stock_code`          | string         | 是   | 股票代码           |
| `side`                | enum           | 是   | `BUY` / `SELL` |
| `order_price`         | decimal string | 是   | 委托价格           |
| `order_quantity`      | int            | 是   | 委托数量           |
| `filled_quantity`     | int            | 是   | 已成交数量         |
| `remaining_quantity`  | int            | 是   | 剩余数量           |
| `avg_fill_price`      | decimal string | 否   | 加权成交均价       |
| `status`              | enum           | 是   | 指令状态           |
| `submitted_at`        | datetime       | 是   | 提交时间           |

### 4.4 成交记录 `TradeRecord`

| 字段               | 类型           | 必填 | 说明         |
| ------------------ | -------------- | ---- | ------------ |
| `trade_id`       | string         | 是   | 成交编号     |
| `stock_code`     | string         | 是   | 股票代码     |
| `buy_order_id`   | string         | 是   | 买方指令编号 |
| `sell_order_id`  | string         | 是   | 卖方指令编号 |
| `trade_price`    | decimal string | 是   | 成交价格     |
| `trade_quantity` | int            | 是   | 成交数量     |
| `traded_at`      | datetime       | 是   | 成交时间     |

### 4.5 账户资金 `FundAccount`

| 字段                 | 类型           | 必填 | 说明                   |
| -------------------- | -------------- | ---- | ---------------------- |
| `fund_account_id`  | string         | 是   | 资金账户号             |
| `investor_id`      | string         | 是   | 投资者编号             |
| `bank_card_no`     | string         | 否   | 银行卡号，响应中应脱敏 |
| `available_amount` | decimal string | 是   | 可用资金               |
| `frozen_amount`    | decimal string | 是   | 冻结资金               |
| `total_amount`     | decimal string | 是   | 总资金                 |
| `status`           | enum           | 是   | 账户状态               |

### 4.6 证券持仓 `SecurityPosition`

| 字段                    | 类型           | 必填 | 说明       |
| ----------------------- | -------------- | ---- | ---------- |
| `security_account_id` | string         | 是   | 证券账户号 |
| `investor_id`         | string         | 是   | 投资者编号 |
| `stock_code`          | string         | 是   | 股票代码   |
| `stock_name`          | string         | 否   | 股票名称   |
| `total_quantity`      | int            | 是   | 总持仓     |
| `available_quantity`  | int            | 是   | 可卖数量   |
| `frozen_quantity`     | int            | 是   | 冻结数量   |
| `cost_price`          | decimal string | 否   | 持仓成本   |

## 5. 交易客户端调用约定

交易客户端原则上不向其他组提供后端服务；它是主要调用方。客户端组应按本文件调用 ACCOUNT、TRADE、INFO、ADMIN 的 HTTP 与 WebSocket 接口。

客户端必须实现：

- 登录后保存账户 token、`fund_account_id`、`security_account_id`。
- 下单前读取行情与涨跌停边界，买入先冻结资金，卖出先冻结证券持仓。
- 下单和撤单通过中央交易系统接口完成。
- 登录成功后建立中央交易系统 WebSocket，接收撮合回报。
- 收到成交、撤单、过期回报后刷新资金与持仓。

## 6. 账户业务子系统接口

基础前缀：`/api/v1/account`

### 6.1 登录认证

`POST /auth/login`

调用方：CLIENT

请求：

```json
{
  "fund_account_id": "FUND000001",
  "password": "encrypted-or-plain-over-https",
  "client_time": "2026-05-25T10:30:00+08:00"
}
```

响应 `data`：

```json
{
  "verified": true,
  "investor_id": "INV000001",
  "fund_account_id": "FUND000001",
  "security_account_id": "SEC000001",
  "first_login": false,
  "token": "jwt-token",
  "expires_at": "2026-05-25T18:30:00+08:00"
}
```

### 6.2 修改资金账户密码

`POST /auth/password`

调用方：CLIENT

说明：资金账户存在两类密码，`password_type` 必须明确指定要修改的密码类型，统一规范为 `TRADE` 或 `WITHDRAW`。

请求：

```json
{
  "fund_account_id": "FUND000001",
  "password_type": "TRADE",
  "old_password": "old",
  "new_password": "new"
}
```

字段说明：

| 字段 | 必填 | 说明 |
| --- | --- | --- |
| `fund_account_id` | 是 | 资金账户号 |
| `password_type` | 是 | 密码类型：`TRADE` 表示交易密码，`WITHDRAW` 表示取款密码 |
| `old_password` | 是 | 原密码 |
| `new_password` | 是 | 新密码 |

### 6.3 查询资金账户

`GET /fund-accounts/{fund_account_id}`

调用方：CLIENT、TRADE、ADMIN

响应 `data`：`FundAccount`

### 6.4 冻结资金

`POST /fund-accounts/{fund_account_id}/freeze`

调用方：CLIENT 或 TRADE，推荐由 CLIENT 下单前调用，TRADE 结算前可再次校验。

请求：

```json
{
  "order_id": "ORD202605250001",
  "amount": "1000.00",
  "reason": "BUY_ORDER",
  "requested_at": "2026-05-25T10:30:00+08:00"
}
```

响应 `data`：

```json
{
  "freeze_id": "FRZ000001",
  "fund_account_id": "FUND000001",
  "frozen_amount": "1000.00",
  "available_amount": "9000.00"
}
```

### 6.5 释放资金

`POST /fund-accounts/{fund_account_id}/release`

调用方：CLIENT、TRADE

请求：

```json
{
  "order_id": "ORD202605250001",
  "amount": "500.00",
  "reason": "CANCELLED"
}
```

### 6.6 资金结算

`POST /fund-accounts/{fund_account_id}/settlements`

调用方：TRADE

请求：

```json
{
  "message_id": "MSG202605250001",
  "trade_id": "TRD202605250001",
  "order_id": "ORD202605250001",
  "change_type": "DEDUCT",
  "amount": "505.00",
  "reason": "TRADE_FILLED"
}
```

### 6.7 查询证券持仓

`GET /security-accounts/{security_account_id}/positions`

调用方：CLIENT、TRADE、ADMIN

可选查询参数：`stock_code`

响应 `data`：

```json
{
  "security_account_id": "SEC000001",
  "positions": []
}
```

### 6.8 冻结证券持仓

`POST /security-accounts/{security_account_id}/positions/freeze`

调用方：CLIENT 或 TRADE

请求：

```json
{
  "order_id": "ORD202605250002",
  "stock_code": "600000",
  "quantity": 100,
  "reason": "SELL_ORDER"
}
```

### 6.9 释放证券持仓

`POST /security-accounts/{security_account_id}/positions/release`

调用方：CLIENT、TRADE

请求：

```json
{
  "order_id": "ORD202605250002",
  "stock_code": "600000",
  "quantity": 40,
  "reason": "CANCELLED"
}
```

### 6.10 证券结算

`POST /security-accounts/{security_account_id}/positions/settlements`

调用方：TRADE

请求：

```json
{
  "message_id": "MSG202605250002",
  "trade_id": "TRD202605250001",
  "order_id": "ORD202605250001",
  "stock_code": "600000",
  "change_type": "INCREASE",
  "quantity": 100,
  "reason": "TRADE_FILLED"
}
```

### 6.11 账户关联查询

`GET /associations`

调用方：CLIENT、TRADE、ADMIN

查询参数：

| 参数                    | 说明       |
| ----------------------- | ---------- |
| `fund_account_id`     | 资金账户号 |
| `security_account_id` | 证券账户号 |
| `investor_id`         | 投资者编号 |

响应 `data`：

```json
{
  "association_id": "ASC000001",
  "investor_id": "INV000001",
  "fund_account_id": "FUND000001",
  "security_account_id": "SEC000001",
  "association_status": "ACTIVE",
  "associated_at": "2026-05-25T10:30:00+08:00"
}
```

状态说明：

| `association_status` | 说明 |
| --- | --- |
| `ACTIVE` | 资金账户与证券账户已建立有效绑定关系 |
| `UNLINKED` | 资金账户与证券账户未建立绑定关系，或查询条件未命中有效关联记录 |

未关联响应示例：

```json
{
  "association_id": null,
  "investor_id": "INV000001",
  "fund_account_id": "FUND000001",
  "security_account_id": "SEC000009",
  "association_status": "UNLINKED",
  "associated_at": null
}
```

### 6.12 账户关联业务校验

`GET /associations/check`

调用方：CLIENT、TRADE

用途：下单、撤单、结算、账户冻结前校验证券账户与资金账户是否满足一对一绑定，并判断当前业务是否允许继续。由于本系统定义证券账户和资金账户必须一对一绑定，所以该接口由 ACCOUNT 统一返回校验结果，CLIENT 和 TRADE 不再自行推断绑定关系。

查询参数：

| 参数 | 必填 | 说明 |
| --- | --- | --- |
| `fund_account_id` | 是 | 资金账户号 |
| `security_account_id` | 是 | 证券账户号 |
| `operation_type` | 是 | 当前业务类型：`BUY_ORDER`、`SELL_ORDER`、`CANCEL_ORDER`、`SETTLEMENT`、`ACCOUNT_FREEZE` |
| `investor_id` | 否 | 投资者编号，用于进一步校验账户归属 |

响应 `data`：

```json
{
  "fund_account_id": "FUND000001",
  "security_account_id": "SEC000001",
  "investor_id": "INV000001",
  "is_related": true,
  "is_unique_valid": true,
  "allow_operation": true,
  "fund_account_status": "NORMAL",
  "security_account_status": "NORMAL",
  "reason": null
}
```

字段说明：

| 字段 | 说明 |
| --- | --- |
| `is_related` | 当前资金账户与证券账户是否已经建立绑定关系 |
| `is_unique_valid` | 是否满足一对一且唯一有效；若一个证券账户绑定多个资金账户或一个资金账户绑定多个证券账户，则为 `false` |
| `allow_operation` | 当前 `operation_type` 是否允许继续 |
| `fund_account_status` | 资金账户当前状态 |
| `security_account_status` | 证券账户当前状态 |
| `reason` | 不允许继续时的中文原因 |

失败示例：账户未绑定。

```json
{
  "fund_account_id": "FUND000001",
  "security_account_id": "SEC000009",
  "investor_id": "INV000001",
  "is_related": false,
  "is_unique_valid": false,
  "allow_operation": false,
  "fund_account_status": "NORMAL",
  "security_account_status": "NORMAL",
  "reason": "资金账户与证券账户未建立绑定关系"
}
```

失败示例：账户状态不允许。

```json
{
  "fund_account_id": "FUND000001",
  "security_account_id": "SEC000001",
  "investor_id": "INV000001",
  "is_related": true,
  "is_unique_valid": true,
  "allow_operation": false,
  "fund_account_status": "FROZEN",
  "security_account_status": "NORMAL",
  "reason": "资金账户已冻结，不能执行当前业务"
}
```

### 6.13 账户状态校验

`POST /status/check`

调用方：CLIENT、TRADE、ADMIN

请求：

```json
{
  "account_type": "FUND",
  "account_id": "FUND000001",
  "operation_type": "BUY_ORDER",
  "checked_at": "2026-05-25T10:30:00+08:00"
}
```

响应 `data`：

```json
{
  "account_type": "FUND",
  "account_id": "FUND000001",
  "status": "NORMAL",
  "allowed": true,
  "reason": null
}
```

## 7. 中央交易系统接口

基础前缀：`/api/v1/trade`

### 7.1 提交买卖指令

`POST /orders`

调用方：CLIENT

请求：

```json
{
  "client_order_id": "CLI202605250001",
  "investor_id": "INV000001",
  "fund_account_id": "FUND000001",
  "security_account_id": "SEC000001",
  "stock_code": "600000",
  "side": "BUY",
  "order_price": "10.10",
  "order_quantity": 100,
  "submitted_at": "2026-05-25T10:30:00+08:00"
}
```

响应 `data`：

```json
{
  "order_id": "ORD202605250001",
  "status": "ACCEPTED",
  "accepted_at": "2026-05-25T10:30:00+08:00"
}
```

校验规则：

- 必填字段缺失返回 `TRADE_E01`。
- 股票不存在返回 `TRADE_E02`。
- 股票暂停或收盘返回 `TRADE_E03`。
- 买卖方向不是 `BUY` / `SELL` 返回 `TRADE_E04`。
- 买入价格高于涨停价、卖出价格低于跌停价、价格小于等于 0 返回 `TRADE_E05`。
- 数量小于等于 0 返回 `TRADE_E06`。

### 7.2 查询指令

`GET /orders/{order_id}`

调用方：CLIENT、ADMIN

响应 `data`：`TradeOrder`

`GET /orders?investor_id=INV000001&page=1&page_size=20`

调用方：CLIENT

### 7.3 撤单

`POST /orders/{order_id}/cancel`

调用方：CLIENT

请求：

```json
{
  "investor_id": "INV000001",
  "cancelled_at": "2026-05-25T10:35:00+08:00"
}
```

响应 `data`：

```json
{
  "order_id": "ORD202605250001",
  "status": "CANCELLED",
  "cancelled_quantity": 60,
  "released_resource_type": "FUND",
  "released_amount": "606.00",
  "released_quantity": null
}
```

仅 `QUEUED` 或 `PARTIALLY_FILLED` 且剩余数量大于 0 的指令允许撤销。

### 7.4 查询订单簿

`GET /order-books/{stock_code}`

调用方：ADMIN

响应 `data`：

```json
{
  "stock_code": "600000",
  "buy_levels": [
    {"price": "10.10", "quantity": 1000}
  ],
  "sell_levels": [
    {"price": "10.20", "quantity": 800}
  ],
  "updated_at": "2026-05-25T10:30:00+08:00"
}
```

### 7.5 查询行情快照

`GET /market/{stock_code}`

调用方：CLIENT、INFO、ADMIN

响应 `data`：`MarketQuote`

`GET /market?stock_codes=600000,000001`

调用方：INFO、ADMIN

### 7.6 交易日开始

`POST /trading-days/open`

调用方：ADMIN

请求：

```json
{
  "trade_date": "2026-05-25",
  "operator_admin_id": "ADM000001"
}
```

### 7.7 交易日结束

`POST /trading-days/close`

调用方：ADMIN 或系统定时任务

请求：

```json
{
  "trade_date": "2026-05-25",
  "trigger_type": "MANUAL",
  "operator_admin_id": "ADM000001",
  "reason": "正常收盘"
}
```

收盘后中央交易系统必须：

- 拒绝新指令。
- 将排队中、部分成交的剩余数量标记为过期。
- 生成过期反馈。
- 向账户系统释放剩余冻结资金或冻结证券。
- 归档当日行情。

### 7.8 管理端设置涨跌停

`PUT /stocks/{stock_code}/limits`

调用方：ADMIN

说明：ADMIN 传入涨跌停比例，TRADE 读取该股票昨日收盘价和股票类型后计算最终涨停价、跌停价，并保存为当日价格限制。CLIENT、INFO、ADMIN 后续只读取 TRADE 保存的最终价格限制，不重复计算。

请求：

```json
{
  "operator_admin_id": "ADM000001",
  "limit_up_ratio": "0.10",
  "limit_down_ratio": "0.10",
  "effective_date": "2026-05-25",
  "reason": "管理员配置"
}
```

响应 `data`：

```json
{
  "stock_code": "600000",
  "previous_close_price": "10.00",
  "limit_up_ratio": "0.10",
  "limit_down_ratio": "0.10",
  "limit_up_price": "11.00",
  "limit_down_price": "9.00",
  "effective_date": "2026-05-25"
}
```

### 7.9 管理端暂停交易

`POST /stocks/{stock_code}/pause`

调用方：ADMIN

请求：

```json
{
  "operator_admin_id": "ADM000001",
  "pause_reason": "交易异常波动",
  "affected_scope": "SINGLE_STOCK"
}
```

### 7.10 管理端重启交易

`POST /stocks/{stock_code}/resume`

调用方：ADMIN

请求：

```json
{
  "operator_admin_id": "ADM000001",
  "resume_reason": "异常解除"
}
```

## 8. 中央交易 WebSocket

### 8.1 交易结果推送

连接地址：`ws://<trade-host>/api/v1/trade/ws/orders?token=<token>`

调用方：CLIENT

通用消息格式：

```json
{
  "type": "order.partially_filled",
  "event_id": "EVT202605250001",
  "request_id": "req-20260525-0001",
  "sent_at": "2026-05-25T10:30:00+08:00",
  "data": {}
}
```

事件类型：

| type                       | 说明           |
| -------------------------- | -------------- |
| `order.accepted`         | 指令已受理     |
| `order.rejected`         | 指令被拒绝     |
| `order.partially_filled` | 部分成交       |
| `order.filled`           | 完全成交       |
| `order.cancelled`        | 撤单成功       |
| `order.expired`          | 交易日结束过期 |

成交回报 `data`：

```json
{
  "order_id": "ORD202605250001",
  "stock_code": "600000",
  "side": "BUY",
  "trade_id": "TRD202605250001",
  "trade_price": "10.05",
  "trade_quantity": 60,
  "filled_quantity": 60,
  "remaining_quantity": 40,
  "order_status": "PARTIALLY_FILLED",
  "traded_at": "2026-05-25T10:30:01+08:00"
}
```

### 8.2 行情推送

连接地址：`ws://<trade-host>/api/v1/trade/ws/market?stock_codes=600000,000001`

调用方：INFO、ADMIN，也可供 CLIENT 使用。

事件类型：

| type               | 说明                        |
| ------------------ | --------------------------- |
| `market.updated` | 行情更新，建议 5 秒推送一次 |
| `stock.paused`   | 股票暂停交易                |
| `stock.resumed`  | 股票恢复交易                |

行情事件 `data`：`MarketQuote`

### 8.3 心跳

客户端每 30 秒发送：

```json
{"type": "ping", "sent_at": "2026-05-25T10:30:00+08:00"}
```

服务端返回：

```json
{"type": "pong", "sent_at": "2026-05-25T10:30:00+08:00"}
```

客户端断线后应指数退避重连。

## 9. 网上信息发布接口

基础前缀：`/api/v1/info`

### 9.1 注册

`POST /auth/register`

调用方：INFO 前端

请求：

```json
{
  "username": "user001",
  "phone": "13800000000",
  "password": "password"
}
```

### 9.2 登录

`POST /auth/login`

请求：

```json
{
  "username": "user001",
  "password": "password"
}
```

连续错误 5 次锁定 15 分钟。

### 9.3 登出

`POST /auth/logout`

调用方：INFO 前端

成功后前端应清除本地敏感数据，断开 WebSocket，停止轮询。

### 9.4 修改密码与找回密码

`POST /auth/password/change`

请求：

```json
{
  "old_password": "old",
  "new_password": "new"
}
```

`POST /auth/password/reset`

请求：

```json
{
  "phone": "13800000000",
  "sms_code": "123456",
  "new_password": "new"
}
```

### 9.5 用户升级 VIP

`POST /users/me/upgrade`

请求：

```json
{
  "package_type": "MONTHLY",
  "payment_channel": "MOCK_PAY"
}
```

响应成功后用户权限级别变为 `VIP`。

### 9.6 股票搜索

`GET /stocks?keyword=浦发`

调用方：INFO 前端、CLIENT

响应 `data`：

```json
{
  "items": [
    {
      "stock_code": "600000",
      "stock_name": "浦发银行"
    }
  ]
}
```

支持股票代码精确查询和股票名称模糊查询。

### 9.7 实时行情查询

`GET /stocks/{stock_code}/quote`

调用方：INFO 前端、CLIENT

响应 `data`：`MarketQuote`

INFO 服务应从 TRADE 的行情接口或行情 WebSocket 同步数据，并本地缓存。刷新频率按报告约定为 5 秒/次。

### 9.8 K 线查询

`GET /stocks/{stock_code}/klines?period=DAY&start_date=2026-05-01&end_date=2026-05-25`

调用方：INFO 前端

权限：VIP 用户可访问。普通用户访问返回 `INFO_VIP_REQUIRED`。

响应 `data`：

```json
{
  "stock_code": "600000",
  "period": "DAY",
  "items": [
    {
      "time": "2026-05-25",
      "open_price": "10.00",
      "close_price": "10.20",
      "high_price": "10.50",
      "low_price": "9.90",
      "volume": 100000
    }
  ]
}
```

## 10. 交易系统管理接口

基础前缀：`/api/v1/admin`

### 10.1 管理员登录

`POST /auth/login`

请求：

```json
{
  "username": "admin001",
  "password": "password",
  "captcha": "1234"
}
```

响应 `data`：

```json
{
  "admin_id": "ADM000001",
  "role": "SENIOR_ADMIN",
  "authorized_stocks": ["600000"],
  "token": "jwt-token",
  "expires_at": "2026-05-25T18:30:00+08:00"
}
```

账号状态支持 `active`、`locked`、`disabled`。连续失败达到阈值后锁定 5 分钟。

### 10.2 管理员修改密码

`POST /auth/password`

请求：

```json
{
  "old_password": "old",
  "new_password": "new",
  "confirm_password": "new"
}
```

成功后强制当前会话退出。

### 10.3 管理端股票查询

`GET /stocks?keyword=600000`

调用方：ADMIN 前端

ADMIN 服务应根据管理员授权股票范围过滤结果，并可从 TRADE 获取实时行情。

### 10.4 查询股票实时行情

`GET /stocks/{stock_code}/quote`

调用方：ADMIN 前端

响应 `data`：`MarketQuote`

### 10.5 设置涨跌停

`PUT /stocks/{stock_code}/limits`

调用方：ADMIN 前端

ADMIN 服务校验角色与授权范围后，向 TRADE 的 `PUT /api/v1/trade/stocks/{stock_code}/limits` 传递涨跌停比例。最终涨停价、跌停价由 TRADE 根据比例、昨日收盘价和股票类型计算。

请求：

```json
{
  "limit_up_ratio": "0.10",
  "limit_down_ratio": "0.10",
  "effective_date": "2026-05-25",
  "reason": "日常配置"
}
```

响应 `data`：

```json
{
  "stock_code": "600000",
  "limit_up_ratio": "0.10",
  "limit_down_ratio": "0.10",
  "limit_up_price": "11.00",
  "limit_down_price": "9.00",
  "effective_date": "2026-05-25"
}
```

### 10.6 暂停交易

`POST /stocks/{stock_code}/pause`

请求：

```json
{
  "pause_reason": "交易异常波动",
  "affected_scope": "SINGLE_STOCK"
}
```

ADMIN 服务校验权限后调用 TRADE 暂停接口，并写入审计日志。

### 10.7 重启交易

`POST /stocks/{stock_code}/resume`

请求：

```json
{
  "resume_reason": "异常解除"
}
```

### 10.8 管理员权限管理

`GET /admins`

调用方：SYSTEM_ADMIN

`PUT /admins/{target_admin_id}/permissions`

请求：

```json
{
  "role": "SENIOR_ADMIN",
  "authorized_stocks": ["600000", "000001"],
  "status": "active",
  "reason": "调整授权范围"
}
```

### 10.9 审计日志查询

`GET /audit/logs?admin_id=ADM000001&operation_type=TRADE_CONTROL&start_time=2026-05-25T00:00:00+08:00&end_time=2026-05-25T23:59:59+08:00&page=1&page_size=20`

调用方：AUDIT_ADMIN、SYSTEM_ADMIN

响应 `data.items[]`：

```json
{
  "log_id": "LOG000001",
  "admin_id": "ADM000001",
  "operation_type": "TRADE_CONTROL",
  "target": "600000",
  "result": "SUCCESS",
  "created_at": "2026-05-25T10:30:00+08:00"
}
```

## 11. 异步消息约定

课程项目可先用 HTTP 同步接口完成联调；如果使用 RabbitMQ，则按以下事件名和消息体约定。

### 11.1 通用消息信封

```json
{
  "event_id": "EVT202605250001",
  "event_type": "trade.settlement.created",
  "source": "trade-service",
  "occurred_at": "2026-05-25T10:30:00+08:00",
  "data": {}
}
```

### 11.2 TRADE -> ACCOUNT：结算消息

Exchange：`trade.events`

Routing key：`trade.settlement.created`

`data`：

```json
{
  "message_id": "MSG202605250001",
  "trade_id": "TRD202605250001",
  "stock_code": "600000",
  "buy_order_id": "ORD-BUY-001",
  "sell_order_id": "ORD-SELL-001",
  "trade_price": "10.05",
  "trade_quantity": 100,
  "buyer_fund_account_id": "FUND-BUY-001",
  "buyer_security_account_id": "SEC-BUY-001",
  "seller_fund_account_id": "FUND-SELL-001",
  "seller_security_account_id": "SEC-SELL-001"
}
```

账户系统处理后应保证：

- 买方资金扣减，买方证券增加。
- 卖方证券扣减，卖方资金增加。
- 释放剩余冻结资金或冻结证券。
- 幂等处理同一 `message_id`。

### 11.3 TRADE -> ACCOUNT：资源释放

Routing key：`trade.resource.release_requested`

`data`：

```json
{
  "order_id": "ORD202605250001",
  "resource_type": "FUND",
  "fund_account_id": "FUND000001",
  "security_account_id": null,
  "stock_code": null,
  "amount": "505.00",
  "quantity": null,
  "reason": "CANCELLED"
}
```

### 11.4 ACCOUNT -> TRADE：结算确认

Exchange：`account.events`

Routing key：`account.settlement.confirmed`

`data`：

```json
{
  "message_id": "MSG202605250001",
  "trade_id": "TRD202605250001",
  "status": "CONFIRMED",
  "failed_reason": null,
  "confirmed_at": "2026-05-25T10:30:02+08:00"
}
```

### 11.5 TRADE -> INFO/ADMIN：行情更新

Routing key：`trade.market.updated`

`data`：`MarketQuote`

发布频率：建议 5 秒/次，或每笔成交后更新并由 INFO 聚合为 5 秒展示节奏。

### 11.6 TRADE -> CLIENT：交易反馈

优先通过 WebSocket 推送；如使用消息队列，则 Routing key：`trade.order.feedback`

`data` 同第 8.1 的成交回报格式。

## 12. 典型跨组调用流程

### 12.1 买入指令

1. CLIENT 调用 ACCOUNT `/auth/login` 完成投资者登录。
2. CLIENT 调用 INFO `/stocks/{stock_code}/quote` 或 TRADE `/market/{stock_code}` 获取行情。
3. CLIENT 调用 ACCOUNT `/fund-accounts/{fund_account_id}/freeze` 冻结资金。
4. CLIENT 调用 TRADE `/orders` 提交买入指令。
5. TRADE 校验股票状态、涨跌停、交易时间后入订单簿并撮合。
6. TRADE 通过 WebSocket 向 CLIENT 推送受理、部分成交、完全成交等回报。
7. TRADE 调用 ACCOUNT 结算接口或发送结算消息。
8. CLIENT 收到回报后刷新 ACCOUNT 资金和证券持仓。

### 12.2 卖出指令

1. CLIENT 查询 ACCOUNT 持仓。
2. CLIENT 调用 ACCOUNT `/security-accounts/{security_account_id}/positions/freeze` 冻结可卖持仓。
3. CLIENT 调用 TRADE `/orders` 提交卖出指令。
4. TRADE 撮合后生成成交记录、结算消息和行情更新。
5. ACCOUNT 完成卖方证券扣减、卖方资金增加。

### 12.3 撤单

1. CLIENT 调用 TRADE `/orders/{order_id}/cancel`。
2. TRADE 判断指令是否为 `QUEUED` 或 `PARTIALLY_FILLED`。
3. TRADE 从订单簿移除剩余数量。
4. TRADE 调用 ACCOUNT 释放资金或证券。
5. TRADE 通过 WebSocket 推送 `order.cancelled`。

### 12.4 管理员暂停股票

1. ADMIN 前端调用 ADMIN `/stocks/{stock_code}/pause`。
2. ADMIN 校验管理员角色与授权股票范围。
3. ADMIN 调用 TRADE `/stocks/{stock_code}/pause`。
4. TRADE 将股票状态改为 `PAUSED`，拒收该股票新指令。
5. TRADE 通过行情 WebSocket 或事件广播 `stock.paused`。
6. ADMIN 写入审计日志。

### 12.5 网上信息发布行情刷新

1. INFO 默认每 5 秒通过 TRADE `/market` 批量 REST 拉取行情。
2. INFO 每 5 秒更新本地行情缓存。
3. 普通用户可查询基础行情。
4. VIP 用户可查询日/月/年 K 线。
5. TRADE 的行情 WebSocket 可作为后续优化，但不是第一版联调必需项。

## 13. 联调要求

- 每组服务必须提供 `/health`：

```json
{
  "service": "trade-service",
  "status": "UP",
  "time": "2026-05-25T10:30:00+08:00"
}
```

- 每组服务必须提供 FastAPI `/docs` 和 `/openapi.json`。
- 写接口必须支持 `X-Request-Id`，日志中记录请求 ID。
- 下单、冻结、释放、结算、交易控制接口必须保证幂等。
- 密码、银行卡、身份证件号不得明文写入日志。
- 任何跨组字段变更必须同步更新本文档和 OpenAPI。

## 14. 已确认的跨组接口规则

以下规则根据当前讨论结果写入 V2，作为五组联调默认执行规则。

1. 资金/证券冻结与账户冻结流程

   - 买入/卖出委托的资源冻结仍采用 CLIENT 前置冻结：买入前 CLIENT 调 ACCOUNT 冻结资金，卖出前 CLIENT 调 ACCOUNT 冻结证券持仓，TRADE 接收订单后可复核。
   - 如果是冻结整个账户 A，CLIENT 先向 TRADE 发起账户 A 的未完成订单撤单/清理请求；TRADE 将账户 A 的未完成订单全部撤单并返回结果；CLIENT 再调用 ACCOUNT 执行账户冻结。
   - 这样可以避免账户被冻结后仍有排队订单继续撮合。

2. 行情同步方式

   - 第一版联调采用 INFO 每 5 秒 REST 拉取 TRADE 行情接口。
   - TRADE WebSocket 行情推送保留为后续优化方案，不作为第一版必需接口。
   - 该同步只用于最新成交价、成交量、最高价、最低价等行情展示，不涉及涨跌停价格的重新制定。

3. 涨跌停配置方式

   - ADMIN 向 TRADE 传涨跌停比例，例如普通股 `0.10`、ST 股 `0.05`。
   - TRADE 根据比例、昨日收盘价和股票类型计算最终涨停价、跌停价，并保存为当日价格限制。
   - CLIENT、INFO、ADMIN 只读取 TRADE 保存的最终价格限制，用于校验或展示。

4. INFO 用户体系

   - INFO 独立维护普通/VIP 用户体系。
   - INFO 用户不需要与 ACCOUNT 的资金账户、证券账户联动。
   - INFO 的登录、注册、VIP 升级、密码找回由 INFO 自己实现。

5. RabbitMQ 使用范围

   - HTTP/WebSocket 是必需接口。
   - RabbitMQ 作为异步实现选项，不要求第一版必须引入。
   - 若后续引入 RabbitMQ，优先用于 TRADE -> ACCOUNT 的结算消息、TRADE -> INFO 的行情事件、TRADE -> CLIENT 的交易反馈备份通道。
