# 股票交易系统 · 账户业务子系统
## 字段与状态码规范文档


---

## 目录
1. [状态码枚举](#1-状态码枚举)
2. [核心数据表字段](#2-核心数据表字段)
3. [JSON数据格式示例](#3-json数据格式示例)
4. [统一API响应格式](#4-统一api响应格式)

---

## 1. 状态码枚举

### 1.1 账户状态 `accountStatus`
> 证券账户与资金账户共用同一套状态码

| 状态码 | 中文含义 | 触发条件 |
|--------|----------|---------|
| `NORMAL` | 正常 | 开户成功 / 补办解挂成功 / 解冻成功 |
| `LOST` | 挂失 | 挂失申请提交并确认 |
| `FROZEN` | 冻结 | 异常控制 / 司法冻结 / 人工冻结 / 密码异常 / 关联限制 |
| `CANCELLED` | 销户 | 满足销户条件后注销 |

**证券账户销户条件：** 证券清空 + 身份核验通过  
**资金账户销户条件：** 余额清零 + 解除证券账户关联

> **跨组接口映射说明**：跨组接口文档（`接口.md`）使用 `CLOSED` 表示账户关闭。本系统内部库表使用 `CANCELLED` 表示投资者主动销户。对外提供接口时，`CANCELLED` 映射为 `CLOSED`。

---

### 1.2 申请状态 `applyStatus`

| 状态码 | 中文含义 | 说明 |
|--------|----------|------|
| `PENDING_SUBMIT` | 待提交 | 草稿，尚未提交 |
| `PENDING_APPROVE` | 待审批 | 已提交，等待审批人处理 |
| `APPROVED` | 已通过 | 审批通过 |
| `REJECTED` | 已驳回 | 审批不通过 |
| `WITHDRAWN` | 已撤销 | 申请人主动撤销 |

---

### 1.3 处理状态 `processStatus`

| 状态码 | 中文含义 |
|--------|----------|
| `WAITING` | 待处理 |
| `PROCESSING` | 处理中 |
| `DONE` | 已完成 |
| `FAILED` | 已失败 |

---

### 1.4 审批状态 `approvalStatus`

| 状态码 | 中文含义 |
|--------|----------|
| `PENDING` | 待审批 |
| `APPROVED` | 已通过 |
| `REJECTED` | 已拒绝 |

---

### 1.5 关联状态 `associationStatus`

| 状态码 | 中文含义 |
|--------|----------|
| `LINKED` | 已关联 |
| `UNLINKED` | 已解除 |

---

### 1.6 交易类型 `transactionType`

| 状态码 | 中文含义 |
|--------|----------|
| `DEPOSIT` | 存款 |
| `WITHDRAW` | 取款 |

---

### 1.7 交易状态 `transactionStatus`

| 状态码 | 中文含义 |
|--------|----------|
| `PROCESSING` | 受理中 |
| `SUCCESS` | 成功 |
| `FAILED` | 失败 |
| `REVERSED` | 冲正 |

---

### 1.8 账户类型 `accountType`

| 状态码 | 中文含义 |
|--------|----------|
| `SECURITIES` | 证券账户 |
| `FUND` | 资金账户 |

---

### 1.9 证件类型 `idType`

| 状态码 | 中文含义 |
|--------|----------|
| `ID_CARD` | 居民身份证 |
| `PASSPORT` | 护照 |
| `HK_MACAO_TAIWAN` | 港澳台证件 |

---

### 1.10 操作类型 `operationType`

| 状态码 | 中文含义 |
|--------|----------|
| `OPEN_ACCOUNT` | 开户 |
| `LOST` | 挂失 |
| `REISSUE` | 补办 |
| `CANCEL` | 销户 |
| `QUERY` | 查询 |
| `DEPOSIT` | 存款 |
| `WITHDRAW` | 取款 |
| `CHANGE_PWD` | 密码修改 |
| `LINK` | 关联 |
| `APPROVE` | 审批 |
| `STATUS_CHANGE` | 状态变更 |
| `ID_VERIFY` | 身份校验 |

---

### 1.11 状态变更类型 `statusChangeType`

| 状态码 | 中文含义 |
|--------|----------|
| `FREEZE` | 冻结 |
| `UNFREEZE` | 解冻 |
| `LOST` | 挂失 |
| `UNLOST` | 解除挂失 |
| `CANCEL` | 销户 |

---

### 1.12 申请业务类型 `applicationType`

| 状态码 | 中文含义 |
|--------|----------|
| `OPEN_ACCOUNT` | 开户 |
| `LOST` | 挂失 |
| `REISSUE` | 补办 |
| `CANCEL` | 销户 |

---

### 1.13 关联操作类型 `associationOpType`

| 状态码 | 中文含义 |
|--------|----------|
| `LINK` | 建立关联 |
| `UNLINK` | 解除关联 |

---

### 1.14 身份校验状态 `verifyStatus`

| 状态码 | 中文含义 |
|--------|----------|
| `PASSED` | 通过 |
| `FAILED` | 未通过 |

---

### 1.15 密码修改状态 `pwdChangeStatus`

| 状态码 | 中文含义 |
|--------|----------|
| `SUCCESS` | 修改成功 |
| `FAILED` | 修改失败 |

---

## 2. 核心数据表字段

### 2.1 账户申请表 `D01 account_application`

| 字段名 | 类型 | 长度/格式 | 必填 | 说明 |
|--------|------|-----------|------|------|
| `applyId` | String | 8~32位，唯一 | 是 | 申请编号 |
| `investorId` | Integer | >0 | 是 | 投资者编号 |
| `accountType` | Enum | `accountType` | 是 | 账户类型 |
| `applicationType` | Enum | `applicationType` | 是 | **申请业务类型**（开户/挂失/补办/销户） |
| `applyTime` | DateTime | 合法日期时间 | 是 | 申请提交时间 |
| `applyStatus` | Enum | `applyStatus` | 是 | 申请状态 |
| `processStatus` | Enum | `processStatus` | 是 | 处理状态 |

---

### 2.2 证券账户表 `D02 securities_account`

| 字段名 | 类型 | 长度/格式 | 必填 | 说明 |
|--------|------|-----------|------|------|
| `securitiesAccountNo` | String | 8~20位，系统内唯一 | 是 | 证券账户号 |
| `investorId` | Integer | >0 | 是 | 投资者编号 |
| `openTime` | DateTime | 合法日期时间 | 是 | 开户时间 |
| `accountStatus` | Enum | `accountStatus` | 是 | 账户状态 |

---

### 2.3 资金账户表 `D03 fund_account`

| 字段名 | 类型 | 长度/格式 | 必填 | 说明 |
|--------|------|-----------|------|------|
| `fundAccountNo` | String | 8~20位，系统内唯一 | 是 | 资金账户号 |
| `investorId` | Integer | >0 | 是 | 投资者编号 |
| `bankCardNo` | String | 16~19位 | 是 | 银行卡号 |
| `availableBalance` | Decimal | ≥0，保留2位小数 | 是 | **可用资金**（可取款、可交易） |
| `frozenAmount` | Decimal | ≥0，保留2位小数 | 是 | **冻结资金**（委托挂单冻结） |
| `accountStatus` | Enum | `accountStatus` | 是 | 账户状态 |
| `tradePwdDigest` | String | 加密摘要，非明文 | 是 | 交易密码摘要 |
| `withdrawPwdDigest` | String | 加密摘要，非明文 | 是 | 取款密码摘要 |

---

### 2.4 账户关联表 `D04 account_association`

| 字段名 | 类型 | 长度/格式 | 必填 | 说明 |
|--------|------|-----------|------|------|
| `associationId` | String | 8~32位，唯一 | 是 | 关联编号 |
| `securitiesAccountNo` | String | 8~20位 | 是 | 证券账户号 |
| `fundAccountNo` | String | 8~20位 | 是 | 资金账户号 |
| `associationStatus` | Enum | `associationStatus` | 是 | 关联状态 |
| `associationTime` | DateTime | 合法日期时间 | 是 | 关联建立/更新时间 |

---

### 2.5 资金流水表 `D05 transaction_record`

| 字段名 | 类型 | 长度/格式 | 必填 | 说明 |
|--------|------|-----------|------|------|
| `serialNo` | String | 8~32位，唯一 | 是 | 流水编号 |
| `transactionId` | String | 8~32位，唯一 | 是 | 交易编号 |
| `fundAccountNo` | String | 8~20位 | 是 | 资金账户号 |
| `amount` | Decimal | >0，保留2位小数 | 是 | 交易金额 |
| `transactionType` | Enum | `transactionType` | 是 | 交易类型 |
| `transactionStatus` | Enum | `transactionStatus` | 是 | 交易状态 |
| `arrivedTime` | DateTime | 合法日期时间，可为空 | 否 | 到账时间 |
| `operateTime` | DateTime | 合法日期时间 | 是 | 操作时间 |

---

### 2.6 审批记录表 `D06 approval_record`

| 字段名 | 类型 | 长度/格式 | 必填 | 说明 |
|--------|------|-----------|------|------|
| `approvalId` | String | 8~32位，唯一 | 是 | 审批编号 |
| `applyId` | String | 8~32位 | 是 | 申请编号 |
| `approvalStatus` | Enum | `approvalStatus` | 是 | 审批状态 |
| `approvalComment` | String | 0~256 | 否 | 审批意见 |
| `approvalTime` | DateTime | 合法日期时间 | 是 | 审批时间 |
| `approver` | String | 1~64 | 是 | 审批人员标识 |

---

### 2.7 操作日志表 `D07 operation_log`

| 字段名 | 类型 | 长度/格式 | 必填 | 说明 |
|--------|------|-----------|------|------|
| `logId` | String | 8~32位，唯一 | 是 | 日志编号 |
| `operator` | String | 1~64 | 是 | 操作人员标识 |
| `operationType` | Enum | `operationType` | 是 | 操作类型 |
| `operateTime` | DateTime | 合法日期时间 | 是 | 操作时间 |
| `operateResult` | Enum/String | `SUCCESS`/`FAILED`/`ERROR` 或失败原因 | 是 | 操作结果 |

---

### 2.8 身份校验记录表 `D08 identity_verify_record`

| 字段名 | 类型 | 长度/格式 | 必填 | 说明 |
|--------|------|-----------|------|------|
| `verifyId` | String | 8~32位，唯一 | 是 | 校验编号 |
| `investorId` | Integer | >0 | 是 | 投资者编号 |
| `idNo` | String | 6~32位，按证件类型校验 | 是 | 证件号码 |
| `verifyStatus` | Enum | `verifyStatus` | 是 | 校验状态 |
| `verifyTime` | DateTime | 合法日期时间 | 是 | 校验时间 |
| `verifyComment` | String | 0~256 | 否 | 校验说明（失败原因） |

---

### 2.9 查询记录表 `D09 query_record`

| 字段名 | 类型 | 长度/格式 | 必填 | 说明 |
|--------|------|-----------|------|------|
| `queryId` | String | 8~32位，唯一 | 是 | 查询编号 |
| `queryCondition` | String | 1~128 | 是 | 查询条件 |
| `queryTime` | DateTime | 合法日期时间 | 是 | 查询时间 |
| `operator` | String | 1~64 | 是 | 操作人员标识 |

---

### 2.10 账户状态变更记录表 `D10 account_status_change`

| 字段名 | 类型 | 长度/格式 | 必填 | 说明 |
|--------|------|-----------|------|------|
| `instructionId` | String | 8~32位，唯一 | 是 | 指令编号 |
| `accountType` | Enum | `accountType` | 是 | 账户类型 |
| `accountNo` | String | 8~20位 | 是 | 证券账户号或资金账户号 |
| `originalStatus` | Enum | `accountStatus` | 是 | 原账户状态 |
| `targetStatus` | Enum | `accountStatus` | 是 | 目标账户状态 |
| `changeReason` | String | 0~256 | 否 | 状态变更原因 |
| `changeTime` | DateTime | 合法日期时间 | 是 | 变更时间 |
| `operator` | String | 1~64 | 是 | 操作人员标识 |

---

## 3. JSON数据格式示例

### 3.1 证券账户
```json
{
  "securitiesAccountNo": "SEC00000001",
  "investorId": 10001,
  "openTime": "2026-05-23T10:00:00",
  "accountStatus": "NORMAL"
}
```

### 3.2 资金账户
```json
{
  "fundAccountNo": "FND00000001",
  "investorId": 10001,
  "bankCardNo": "6222021234567890",
  "availableBalance": 8000.00,
  "frozenAmount": 2000.00,
  "accountStatus": "NORMAL",
  "tradePwdDigest": "<hash_value>",
  "withdrawPwdDigest": "<hash_value>"
}
```

### 3.3 账户关联
```json
{
  "associationId": "ASSOC00000001",
  "securitiesAccountNo": "SEC00000001",
  "fundAccountNo": "FND00000001",
  "associationStatus": "LINKED",
  "associationTime": "2026-05-23T10:00:00"
}
```

### 3.4 资金流水
```json
{
  "serialNo": "SER00000001",
  "transactionId": "TXN00000001",
  "fundAccountNo": "FND00000001",
  "amount": 5000.00,
  "transactionType": "DEPOSIT",
  "transactionStatus": "SUCCESS",
  "arrivedTime": "2026-05-23T10:05:00",
  "operateTime": "2026-05-23T10:00:00"
}
```

### 3.5 账户申请
```json
{
  "applyId": "APP00000001",
  "investorId": 10001,
  "accountType": "SECURITIES",
  "applicationType": "OPEN_ACCOUNT",
  "applyTime": "2026-05-23T10:00:00",
  "applyStatus": "PENDING_APPROVE",
  "processStatus": "WAITING"
}
```

### 3.6 操作日志
```json
{
  "logId": "LOG00000001",
  "operator": "staff_001",
  "operationType": "OPEN_ACCOUNT",
  "operateTime": "2026-05-23T10:00:00",
  "operateResult": "SUCCESS"
}
```

---

## 4. 统一API响应格式

> 建议与其他子系统组对齐此格式

### 成功响应
```json
{
  "code": 200,
  "message": "success",
  "data": { }
}
```

### 失败响应
```json
{
  "code": 400,
  "message": "账户状态异常，无法办理",
  "data": null
}
```

### 常用业务错误码建议

| code | 说明 |
|------|------|
| 200 | 成功 |
| 400 | 请求参数错误 |
| 401 | 未登录或无权限 |
| 403 | 权限不足 |
| 404 | 账户不存在 |
| 409 | 数据冲突（如重复开户） |
| 500 | 系统内部错误 |

