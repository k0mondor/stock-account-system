# API Base URL
VITE_API_BASE_URL=http://localhost:8000

# API Prefix
VITE_ACCOUNT_PREFIX=/api/v1/account

# Shared service token for local integration
VITE_DEV_BEARER_TOKEN=replace-with-a-shared-service-token
